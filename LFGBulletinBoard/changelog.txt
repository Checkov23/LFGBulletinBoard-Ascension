--[[

Group Bulletin Board 	

1.08
- Renamed to "LFG Bulletin Board (Checkov Fork)" so it is distinguishable from
  the upstream addon. The name shows in the AddOns list, in the window title
  and in the options About panel. Author field now reads
  "Vyscî-Whitemane, fork by Checkov": the original attribution stays, which is
  what the BSD licence requires, and the fork is named on top of it.
  The folder name stays LFGBulletinBoard because it has to match the .toc.

1.07
- Addon to addon traffic no longer reaches the board. 3.3.5a cannot send addon
  messages over a chat channel, so addons that want a server wide bus post
  serialized payloads as ordinary chat lines into a hidden channel, for example
  "LC1:CONF:dd2cba74:DW4Uoimmmu0ViIssZZzKqILWGz(...". The player never sees
  those, but a channel scanner does, and random letters inevitably hit the two
  and three letter dungeon abbreviations. One packet was showing up as
  Stratholme, Zul'Aman and all four Scarlet Monastery wings at once, the last
  because "sm" expands to every wing.
  A line is treated as machine traffic when it contains a 20 character unbroken
  alphanumeric run, or starts with two colon separated uppercase tokens. No
  human LFG line does either. Verified against the real payloads and against
  the real requests from the same board, which all still match.

1.06
- THE reason the board stayed empty. Ascension ships a newer FrameXML
  tContains that returns true or NOTHING, where stock 3.3.5a returns true or
  false. Tool.Split only inserted a word when "tContains(t,str)==false", which
  is never true against the newer one, so every keyword list built through
  langSplit came out EMPTY. The only surviving keyword was the single hand
  written literal in Tags.lua, which is why /gbbraw reported exactly
  "keywords=1  keystone->nil".
  All four comparisons now test for truth instead of equality with false.
  Keyword count goes from 1 to 418 and keystone resolves to MPLUS.
- The load test now models Ascension's tContains rather than the stock one, and
  asserts the keyword table is populated. It reproduces the empty board exactly
  before the fix and passes after.
- check335.mjs rejects "tContains(...) == false".

1.05
- Right clicking the board threw in UIDropDownMenu_Initialize. Ascension ships
  a NEWER UIDropDownMenu than stock 3.3.5a (the client has Interface/SharedXML),
  and it concatenates frame:GetName(); the popup menu frame was created
  anonymous, which stock 3.3.5a tolerates in MENU mode. It has a name now.
- /gbbraw also prints a status line now: english keywords on or off, how many
  keywords are loaded, whether "keystone" is one of them, which channels are
  ticked, how many dungeon filters are on, and how many entries the board is
  holding. That answers "why is the board empty" in one screenshot.
- New /gbbfix: turns all 20 channels back on, every dungeon filter on, the level
  filter off, english keywords on, unfolds all headers and rebuilds the keyword
  table. Recovery from the half finished settings that the four crashed startups
  before 1.03 may have written into SavedVariables.

1.04
- Mythic+ recognition widened. A keystone link still wins outright; on top of
  that "mythic", "+11", "M+" and "11 key" are now matched by pattern, because
  the tokeniser destroys punctuation and those can never match as plain words.
  Only a keystone link removes the Trade category, the looser patterns just add
  Mythic+, so "WTS +15" stays a sale.
- CoA category level ranges widened to 1-60. A narrow range only gave the level
  filter a way to hide these categories from the people who want them.
- New /gbbraw command. It prints the next channel lines exactly as the addon
  receives them, pipes doubled so hyperlinks stay readable, together with the
  categories each line matched. For working out why a specific line is not
  picked up instead of guessing.

1.03
- Fixed the options panel crash introduced with the CoA categories. The
  localisation panel reads the English fallback name table behind
  GBB.dungeonNames and concatenates the value with no nil check; the CoA names
  had only been written to the front table. They now go into both.
  That single crash was behind all three reported symptoms: empty window,
  missing minimap button, and only the WotLK instances in the settings, because
  it aborted Init before any of that was built.
- Keystones are posted as an item link. The tokeniser turns the link prefix into
  the word "hitem", which upstream files under Trade, so every keystone request
  was landing there. Keystone lines now drop Trade, pin to Mythic+, and count as
  a request even when the poster writes no role word.
- AscensionContent.lua moved after RequestList.lua in the TOC, since it now
  wraps GBB.GetDungeons.
- New tools/loadtest.mjs: runs the addon in a Lua VM against a stubbed 3.3.5a
  API and asserts that Init completes, the minimap button exists, the Classic
  filter panel is built, and a real keystone line lands in Mythic+.

1.02
- FontString:SetMaxLines removed. Like SetScale before it, that method does
  not exist on a 3.3.5a FontString. It aborted OptionsInit at the channel
  checkboxes, so the TBC and Classic filter panels were never built, and it
  aborted UpdateList, so the window stayed empty. The minimap button was
  missing for the same reason: Init never got that far.
- New GBB.Compat.TryCall for methods that may be absent
- check335.mjs now rejects ANY method sent to a FontString that is not in
  the 3.3.5a FontString API, which is the class of bug behind both crashes
- CoA categories moved from the WotLK block to the Classic block, because
  CoA runs vanilla content
- Classic dungeon filters now default to on. Upstream ships them off, which
  left the board empty on a vanilla-content server
- Version scheme switched to a simple running number

1.01 (published as 2.64-ascension2, renumbered)
- FontString:SetScale removed. SetScale is a Frame method on 3.3.5a, so scaling
  a FontString threw and aborted Init before the minimap button was created.
  All scaling now goes through GBB.Compat.ScaleRegion, which scales frames
  normally and FontStrings through their font height.
  Reported in game on Rexxar (Conquest of Azeroth).
- All comments and messages translated to English

1.00 (published as 2.64-ascension1, renumbered)
Port to WoW 3.3.5a / Project Ascension
- Interface set to 30300
- Compat335.lua: compatibility layer for 3.3.5a
- AscensionContent.lua: Manastorm, Mythic+, CoA dungeons, world bosses
- Removed the events GROUP_JOINED/GROUP_ROSTER_UPDATE/GROUP_LEFT/LOADING_SCREEN_DISABLED
- Replaced C_FriendList, C_Map, IsGuildMember, IsInRaid, UnitFullName, UnitDistanceSquared
- GetChannelList is read as pairs OR triples (Ascension returns pairs)
- ScrollFrame.ScrollBar fetched through _G (the window stayed empty otherwise)
- GetChecked normalised to a real boolean (options reset themselves otherwise)
- GetScrollOffset falls back to GetCurrentScroll
- OnHyperlinkEnter/-Leave only where the frame knows the script
- A GetChannelName result of 0 is treated as "not joined"
- Minimap textures back to paths instead of FileDataIDs
- enableMouseWheel/enableMouseClicks moved out of the XML into Lua
- Heroic level filter no longer hardwired to level 70
- PlaySound with a name instead of a SoundKit ID

2.63
- Improved performance (fingers crossed). All thanks to dkcas11 who made the changes unless it doesn't work then blame him

2.62
- No really, I fixed the bug that broke the addon
- More Chinesse localization
- Seasonal Events

2.61
- Added option to adjust font size. 3 whole options, don't ask for more
- Fixed the thing that made the addon not working for everyone except me 
- LFG bar defaulted to being off (its ugly af)

2.60 
 - I cant remember what happened here. I was drunk

2.59
- Fixed interference with other addons
- Make LFG bar optional (configured in setting)
- Fixed "Whisper" Typo

2.58 
 - Added a text box to post LFG msg at a click of a button

2.57 
 - Added support for arena

2.56
 - Fixed localization channel bug

2.55
 - Filtering by normal dungeon
 - Raid categorization
 - Selecting normal only or heroic only filter will not cause raids to be filtered out
 - Support for Chinese (zhTW Traditional Taiwan) done completely by pallthennonwow, so huge thanks or huge hate to him
 - Removed post history as it's just clutter
 
2.54
 - Fixed addon crashing when custom localization is set
 - Fixed localization not working
 - Updated Russian localization (thanks to hungry)
 - Updated French official channel (thanks to yannlugrin)
 
2.53
 - Fix for heroic not filtering properly when custom in search patterns is checked
 
2.52
 - Added HC to heroics
 - Refactored to allow the option to configure heroic tags
 - Support for french localization thanks to mrbuds

2.51 
 - Added support to filter by heroic only dungeons
 - Supported to select color of heroic and normal dungeons
 - Fixed a bug where custom tags were not working
 - Fixed a bug that was causing classic dungeons in tbc to not display the proper level
 - Fixed a few tags, such as black morass appearing under black temple
 - Turns off classic dungeons by default in TBC
 
2.50 
 - More German localization
 
2.49 
 - Russian localization thanks to the great nourlie
 - German locization thanks to amazing tamariske

2.48
- Semi fixed classic dungeon levels with TBC
- Implemented Ctrl + Click to invite player (credit to: h0tw1r3)
- Fixed bug with Shift + Click (credit to: h0tw1r3)

2.47
- Added a new feature to contain all lfg spam in a single chat tab. Can be accessed via /gbb chat clean/organize. 
- It will create a new chat tab called "LFG", will subscribe player to spammy chat channels like "LookingForGroup" if they are not already subscribed.
- The new chat tab will be subscribed to all channels the user has joined, this will ensure that GBB has a source of msg to parse through
- Finally it will remove spammy lfg channels from the default tab. This allows the user to never have to see the lfg spam in chat and be notified via chat on new filtered requests or view requests in Bulletin Board

- Fixed a bug with /gbb about opening up wrong tab
- Created a seperate changelog file

2.46
- Actually fixed background being transparent on install

2.45 
- Fixed a bug causing issue opening main window due to SM and DM dungeons

2.44
- Add TBC support. 
- Move vanilla dungeons in a seperate panel. 
- Rename to "LFG Bulletin Board" from "Group Bulletin Board" 
- Refactor code. 
- Lua sucks

2.43
- Update to work for tbc pre-patch. Fix bug with background being transparent 

2.42
- toc-Update

2.41
- repair level-display in main window

2.40
- release

2.33
- Optional: Add more info to chat on /who and when somebody comes online

2.32
- optional: Add guild in player tooltip
- optional: Combine sub-dungeons like Dire Maul
- message-history in tooltip

2.31
- optional: Track Party/Members (only when in dungeon)
- click on a name in the member-list to add a note!
- Infos in Tooltip: Last seen (with note and dungeon)

2.30
- update russian by Arrogant_Dreamer 

2.22 
- Option: Notification on world/dungeons/raids/battleground

2.21
- Remove Announce/Announce-Box - doesn't work anymore

2.20 	
- TOC-UPDATETIMER

2.13
- option to change color of message, time and chat notification
- "two line"-design is now selectable

2.12
- detection of minmap-shape
- set stata to low - should now be behind dialogs
- Optional: Short one-line chat notification
- Support for Raid-Symbols {rt1}{cirlce}

2.11
- fix bug with english client
- fix bug with guild messages

2.10
- update russian tags for dire maul (thanks to SD_Liberty)
- Chat-Notifications - first line is now light grey
- some code optimizations
- new option: Scan Guild Channel (in Filter/Channel list)
- Announce/Announce now work with closed window
- Item-Links have now a tooltip
- Adjust mousewheel-scroll-speed to one line

2.07 Beta
- Fix for russian language (db west)

2.06 Beta
- Guild Member are marked with a green dot
- Fix russian /who detection

2.05
- repair "run"-Detection
- repair slash commands

2.04 
- bugfix Hitbox with "ChatStyle"
- option "Don't truncate message"
- When two request have the same time, they are sorted by name.
- updates disabled during combat!
- Friends are marked with a Star

2.03
- option "Don't filter own request"

2.02	
- fix same bug again

2.01
- bugfix

2.00	
- Channel-Filter
- repair priest-icon
- option "Show a fixed number of requests"
- option "Chat Style"
- About-Panel
		
]]--
